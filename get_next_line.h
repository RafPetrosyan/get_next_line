#ifndef GET_NEXT_LINE_H
#define GET_NEXT_LINE_H
//#define BUFFER_SIZE 42

#include <unistd.h>
#include <stdlib.h>

char	*ft_strjoin(char const *s1, char const *s2, int read_size);
int	    ft_check(char *buf);
size_t	ft_strlen(const char *str);

#endif
